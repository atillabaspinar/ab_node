module.exports = class AdminController {
    static getAddArticle(req, res, next) {
        console.log(req.url);
    }

    static postAddArticle(req, res, next) {
        console.log(req.url);
    }

    static postUpdateArticle(req, res, next) {
        console.log(req.url);
    }

    static deleteArticle(req, res, next) {
        console.log(req.url);
    }
}


